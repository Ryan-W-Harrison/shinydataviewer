% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_data_viewer.R
\name{data_viewer_card_ui}
\alias{data_viewer_card_ui}
\title{Data Viewer Module Wrapped in a Card}
\usage{
data_viewer_card_ui(
  id,
  title = NULL,
  full_screen = TRUE,
  sidebar_title = NULL,
  table_controls_position = c("top", "bottom"),
  plot_color = NULL
)
}
\arguments{
\item{id}{Module id.}

\item{title}{Optional card title.}

\item{full_screen}{Whether the wrapper card can enter full screen mode.}

\item{sidebar_title}{Optional title for the variable summary sidebar.}

\item{table_controls_position}{Where table pagination controls should appear.
One of \code{"top"} or \code{"bottom"}.}

\item{plot_color}{Optional CSS color used by the variable-summary mini plots.
Accepts values such as \code{"#2c7fb8"}, \code{"rebeccapurple"}, \code{"rgb(44 127 184)"}, or \code{"var(--bs-success)"}. If \code{NULL}, the active Bootstrap primary
color is used.}
}
\value{
A card containing the module UI.
}
\description{
Data Viewer Module Wrapped in a Card
}
\examples{
ui <- bslib::page_fillable(
  theme = bslib::bs_theme(version = 5),
  bslib::layout_columns(
    col_widths = c(4, 8),
    bslib::card(
      bslib::card_header("Context"),
      bslib::card_body("Supporting content")
    ),
    data_viewer_card_ui(
      "viewer",
      title = "Dataset",
      full_screen = FALSE
    )
  )
)

server <- function(input, output, session) {
  data_viewer_server(
    "viewer",
    data = shiny::reactive(mtcars)
  )
}

if (interactive()) {
  shiny::shinyApp(ui, server)
}
}
