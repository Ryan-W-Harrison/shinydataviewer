% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_data_viewer.R
\name{data_viewer_server}
\alias{data_viewer_server}
\title{Data Viewer Module Server}
\usage{
data_viewer_server(
  id,
  data,
  top_n = 6,
  default_page_size = NULL,
  page_size_options = c(15, 25, 50, 100),
  searchable = TRUE,
  filterable = TRUE,
  sortable = TRUE,
  summary_card_fn = variable_summary_card,
  reactable_theme = NULL,
  default_col_def = NULL,
  reactable_args = list()
)
}
\arguments{
\item{id}{Module id.}

\item{data}{Reactive returning a data frame. Supported column classes are
numeric, integer, character, factor, logical, \code{Date}, and
\code{POSIXct}/\code{POSIXt}, and \code{hms}/\code{difftime}.}

\item{top_n}{Maximum number of categorical levels to keep in compact summary
views before collapsing the remainder into \code{"Other"}.}

\item{default_page_size}{Optional default number of rows to show in the table.
If \code{NULL}, the module shows all rows up to the largest page-size option.}

\item{page_size_options}{Page-size options shown in the table controls.}

\item{searchable}{Whether the table search box is enabled.}

\item{filterable}{Whether column filters are enabled.}

\item{sortable}{Whether column sorting is enabled.}

\item{summary_card_fn}{Function used to render each variable summary card.
It must accept at least \verb{(summary_row, index)} and return a Shiny UI tag.}

\item{reactable_theme}{Optional \code{reactable::reactableTheme()} object. If
\code{NULL}, the module uses its built-in theme.}

\item{default_col_def}{Optional default \code{reactable::colDef()} to apply to all
columns. If \code{NULL}, the module uses its built-in default column definition.}

\item{reactable_args}{Optional named list of additional arguments passed to
\code{reactable::reactable()}. These values override the module defaults.}
}
\value{
Invisibly returns a list of reactives named \code{data} and \code{summary}.
}
\description{
Data Viewer Module Server
}
\examples{
custom_summary_card <- function(summary_row, index) {
  htmltools::tags$div(
    class = "custom-summary-card",
    sprintf("\%s: \%s", summary_row$var_name[[1]], summary_row$type[[1]])
  )
}

ui <- bslib::page_fillable(
  theme = bslib::bs_theme(version = 5),
  data_viewer_card_ui("viewer", title = "Iris")
)

server <- function(input, output, session) {
  data_viewer_server(
    "viewer",
    data = shiny::reactive(iris),
    searchable = TRUE,
    filterable = TRUE,
    sortable = TRUE,
    summary_card_fn = custom_summary_card
  )
}

if (interactive()) {
  shiny::shinyApp(ui, server)
}
}
